# 数据说明

本目录描述 `data/listwise/train.jsonl` 与 `data/listwise/dev.jsonl` 的构造。样本正文不进入版本库。`manifest.json` 记录按本说明生成后的规模。

当前清单：训练集 22,062 条，验证集 1,767 条。每条样本用 Qwen3.5 词表编码后不超过 768 个 token。长度以 4B 与 9B 共用的词表测量。

金标来自原始标注、规则或时间顺序。生成过程不调用其他模型给样本打分。

## 记录格式

```json
{
  "id": "ultrafeedback/015509/flan_v2_flan2021/a9bd58eddf",
  "source": "ultrafeedback",
  "stem": "Rank the candidate answers to the user. Higher score = a better response.",
  "options": ["...", "..."],
  "gold": [1.5, 4.25, 4.25, 5.0],
  "k": 4,
  "qtype": "rank",
  "n_unique": 3,
  "gap": 0.75,
  "span": 3.5
}
```

| 字段 | 含义 |
|---|---|
| `stem` | 题干。模型据此形成比较条件 |
| `options` | 待排序文本，彼此不重复 |
| `gold` | 与 `options` 等长。数值越大，排序越靠前 |
| `qtype` | `rank`、`score`、`choice` 或 `noul` |
| `n_unique` | 金标中不同档位的个数 |
| `gap` | 相邻不同档位的最小间距 |
| `span` | 最高档与最低档之差 |

写入文件前，选项按样本 `id` 的哈希做一次固定置换，金标随选项移动。训练时再做一次随机置换。

一条记录进入语料，需要同时满足：

- 至少两个选项，且选项文本互异；
- 金标至少有两个不同取值；
- 题干、字母编号的选项和末尾 `Answer:` 位置编码后不超过 768 token。

## 来源与金标

### UltraFeedback

来源为 `openbmb/UltraFeedback`。每条回答的分数是四个 Likert 维度的算术平均：instruction following、honesty、truthfulness、helpfulness。四个维度中至少要有三个有分数。不使用 `overall_score`；它与上述四项所确定的赢家不一致的比例约为 46%。

词集合 Jaccard 不低于 0.9 的回答视为近重复并删除。保留下来的列表需要至少三个档位，最小档距不低于 0.5，极差不低于 1.5。若整表超过长度上限，只删除中间名次里最长的回答，不删除唯一的最高分或最低分。

样本 `id` 由数据集下标、子集名称和题干哈希组成。原始数据中的空 `id` 不参与切分。

### HelpSteer2

来源为 `nvidia/HelpSteer2` 的 train 与 validation。同一题干下的回答按人工 `helpfulness` 聚成一条记录。近重复的删除规则同上。最高分与最低分之差至少为 1。一个题干超过四条回答时，保留帮助度最高的四条。

两个档位与三个及以上档位都保留。训练时前者使用 RankNet，后者使用排序相关损失。

### 定额抽取

以下来源从已经带有内在顺序的 jsonl 中按来源定额抽取，再做去重和长度过滤。只有一个正确选项的题目，选项数不超过 6。

| 来源 | 上限 | 金标 |
|---|---:|---|
| numeric | 1000 | 与真值距离的相反数，距离越小分数越高 |
| date | 600 | 时间或逻辑上越早，分数越高 |
| yelp/score | 900 | 评论星级 |
| amazon/score | 900 | 商品评分 |
| sst5/score | 900 | 句子情感档位 |
| legacy_policy/score | 448 | 规则题的分档 |
| mnli/choice | 900 | 正确的前提—假设关系为 1，其余为 0 |
| agnews/choice | 600 | 正确主题为 1 |
| compositional/choice | 700 | 规则允许为 1 |
| boolq/noul | 600 | 与段落一致的是非为 1 |
| trec/choice | 500 | 正确的问题类型为 1 |
| legacy_policy/choice | 224 | 正确决策为 1 |
| legacy_policy/noul | 200 | 规则允许为 1 |

不纳入 `banking77` 与 `dbpedia14`：二者是十几个类别中只有一个正类的单标签分类。不纳入与星级或主题题重复的 yelp、imdb、agnews 是非题。

### 程序生成

每条程序样本都是独立情形，不重复同一事实。

- **步骤复原。** 脚本长度为 5 到 8。打乱后按原顺序打分，越早越高。长于 8 步的脚本只保留首段和末段两个窗口。
- **检索。** 每个事实生成四类比较：答案质量、结果有用程度、片段是否回答查询、查询意图。直接答案高于相关但非答案的文本，无关文本为 0。
- **规则。** 退款窗口、响应时限、年龄、密码和优惠券由条件直接计算允许或不允许。
- **短声明。** 证据与声明的关系为支持、不足或矛盾，正确关系为 1。

## 划分

全局去重键由题干和选项文本组成。验证集按来源分层：

| 来源样本数 | 验证条数 |
|---|---|
| 少于 10 | 0 |
| 10 到 19 | 2 |
| 不少于 20 | 约 10%，且不少于 8 |

`ultrafeedback` 的验证集上限为 600，`helpsteer2` 为 250，避免验证集被单一偏好来源占据。同一来源内按 `id` 哈希排序，取前一段作为验证，而不是随机抽行。划分之后，训练集和验证集分别打乱。

生成后的核对条件：`id` 唯一；`options` 与 `gold` 等长；同一条记录内选项不重复。按此得到的训练集中，三个及以上档位 14,600 条，两个及以下档位 7,462 条。验证集中 UltraFeedback 约占 34%。
