# 语料编制

训练和验证是两个 jsonl：`data/listwise/train.jsonl`、`data/listwise/dev.jsonl`。一行是一道列表排序题。`gold` 越大越应排在前面。分数来自数据自身的档位、时间顺序或人工标注，不用另一个模型现打的标签。

正文不放进仓库。`manifest.json` 是按下面的规则编出来的规模：训练 22062 条，验证 1767 条。每条都能被 Qwen3.5 的分词器装进 768 个 token。长度按 4B 和 9B 共用的词表测量。

## 一行里有什么

```json
{
  "id": "ultrafeedback/015509/flan_v2_flan2021/a9bd58eddf",
  "source": "ultrafeedback",
  "stem": "Rank the candidate answers to the user. Higher score = a better response.\n\n...",
  "options": ["...", "..."],
  "gold": [1.5, 4.25, 4.25, 5.0],
  "k": 4,
  "qtype": "rank",
  "n_unique": 3,
  "gap": 0.75,
  "span": 3.5
}
```

`stem` 是题目，`options` 是待排序的文本。打包时用样本 id 的哈希打乱选项，金标跟着走，避免金标顺序和原数据集顺序绑在一起。训练时还会再打乱一次。

入选条件：

- 选项至少 2 个，文本互不重复，和金标等长。
- 金标至少两个不同的值。
- 用列表编码后不超过 768 token。编码是题目、按字母标出的选项，再加一个 `Answer:` 位置。

## 各来源怎么定分

**UltraFeedback。** 读 `openbmb/UltraFeedback`。每条回答的分数是四个维度的平均：遵循指令、诚实、真实、有帮助。四个里要有至少三个有分。不用 `overall_score`，它和这四项的赢家大约有一半对不上。词重叠达到 0.9 的近重复回答删掉。留下的列表至少三档，最小间距不小于 0.5，最高和最低相差不小于 1.5。超长时只删中间名次里最长的一条，不删唯一的最高分或最低分。id 用数据集下标、来源和题干哈希，避免空 id 把整包来源灌进验证集。

**HelpSteer2。** 读 `nvidia/HelpSteer2` 的 train 和 validation。同一题干下的回答按人工 `helpfulness` 聚成一张表。近重复删掉，最高和最低至少差 1。超过四条时保留帮助度最高的四条。两档和多档都保留，两档在训练里走 RankNet。

**已有的分级题和决策题。** 从早先整理好的 jsonl 里按来源定额抽取，再做一次去重和长度过滤。单赢家题的选项数不超过 6。定额是：

| 来源 | 条数上限 | 金标 |
|---|---|---|
| numeric | 1000 | 与真值的距离，越近越高 |
| date | 600 | 时间或逻辑上的先后 |
| yelp/score、amazon/score、sst5/score | 各 900 | 星级或情感档位 |
| legacy_policy/score | 448 | 规则题上的分档 |
| mnli/choice | 900 | 关系标签，对的为 1 |
| boolq/noul | 600 | 是否题 |
| trec/choice | 500 | 问题类型 |
| agnews/choice | 600 | 新闻主题 |
| compositional/choice | 700 | 组合规则的接受或拒绝 |
| legacy_policy/choice | 224 | 规则选择 |
| legacy_policy/noul | 200 | 规则是否题 |

不收 banking77 和 dbpedia14。它们是十几个选项里只有一个 1 的单标签。也不收和星级题重复的 yelp、imdb、agnews 是否题。

**程序生成。** 每条都是独立情形，不把十几条事实复制几百遍。

- 步骤复原：每条脚本 5 到 8 步，打乱后按原时间顺序打分，越早越高。脚本长于 8 步时只取头尾两个窗口。
- 检索：每个事实生成回答质量、点击有用程度、片段是否答到问题、查询意图。相关但不是答案的文本低于直接答案，无关文本为 0。
- 规则：退款、时限、年龄、密码、优惠券都由数值和条件直接算出允许或不允许。
- 短声明：证据对声明的关系是支持、不足或矛盾，只有正确关系为 1。

## 切分

先按题干和选项做全局去重。再按来源分层切验证集。一个来源少于 10 条时全部留在训练；少于 20 条时验证取 2 条；其余大约取 10%，且至少 8 条。UltraFeedback 的验证最多 600 条，HelpSteer2 最多 250 条，避免验证集变成单一偏好来源。同一来源里按 id 哈希排序后取前一段做验证，不是随机抽行。最后训练和验证各自打乱。

编完要核对：id 不重复，选项数和金标等长，选项文本没有内部重复。当前结果是训练里三档及以上 14600 条、两档及以下 7462 条，验证里 UltraFeedback 约占 34%。
